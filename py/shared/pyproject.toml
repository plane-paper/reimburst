[project]
name = "shared"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.42",
    "pydantic>=2.13",
    "sqlalchemy>=2.0.51",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["shared"]
